function scr_enums(){
enum game_states{
		init,
		button,
		cutscene,
		setting,
	}
enum layers{
	overworld = 0,
	buttons = -100,
	UI = -200,
	textbox = -300,
	textbox_arrow = -400,
	setting = -500,
	fade = -600
}
}